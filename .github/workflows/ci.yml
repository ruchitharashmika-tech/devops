name: Simple CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run a simple command
        run: echo This is my pipeline run trial 3
      
      - name: step 1
        run: echo "This is my pipeline run trial 4"

      - name: step 2
        run: "echo This is my pipeline run trial 5"

      - name: pipeline break
        run: echo "This pipeline is fixed"

      - name: step 3
        run: echo "This is my pipeline run trial 6"
